# SalesForceContacts

// steps i took 
//started a github repo and cloned it
//created 2 folders, front and back
//ran npm install -g @angular/cli to download angular cli to use ng 

                            ///   backend    ////
//in backend ran this line Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass in order to run npm init -y and initiate the backend
//downloaded packages i need: npm install express mongoose cors axios body-parser dotenv

                            ///     db   ////
//npm install express sqlite3 cors body-parser axios dotenv 
// later after mongodb i will try the api sqlite 

                            ///      frontend   ///
// in frontend created the new app: ng new contacts-app --routing --style=scss
// how to run server: node server.js
// how to run angular front: ng serve

                            ///      starting   ///

// now starting to build the mvc architecture and backend ( controller, repo,model, routes, server with sqllite api)
